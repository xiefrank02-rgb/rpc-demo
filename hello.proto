syntax = "proto3";

package hello;

option go_package = "./hello"; // 指定 Go 包路径

// 定义一个请求消息
message HelloRequest {
  string name = 1;
}

// 定义一个响应消息
message HelloReply {
  string message = 1;
}

// 定义一个服务
service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
}
